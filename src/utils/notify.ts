// import * as admin from 'firebase-admin'

// export class Notify {
//   static async sendMessage(token, payload) {
//     return await admin.messaging().sendToDevice(token, payload, {
//       priority: 'high',
//       contentAvailable: true,
//     })
//   }
// }
